# 阅读器后续优化与环境确认

## 2026-09-18 主项目用户验收（最新运行状态）

- 用户明确要求以后只在主项目验收，隔离/子项目只供 AI 自测。已将此规则写入 AGENTS.md。
- 已结束 3285 临时验证服务，从 `D:\project\Ai-judu-deep-reader` 启动主项目，端口 3000，构建目录 `.next`，正式数据目录 `D:\project\Ai-judu-deep-reader\data`。
- `http://localhost:3000/` 首页及 `/api/library` 均 HTTP 200；正式书库启动前后均为 2 本书、2 个版本，没有清空或替换为测试书库。
- 主项目进程 PID 34444，执行会话 67707；3283—3288 验证端口均无监听。后续以实际进程状态为准，不因旧记录重复启动。
- 用户验收地址只提供主项目 3000。以下各节中的“保留 3285 / 主项目未重启”均为历史状态，已被本节替代。

## 用户目标与约束

撤销为内置浏览器白屏增加的改动后重试。以后遇到疑似 Codex、工具或编译验证环境问题，先停止相关工作请用户确认，不为开发工具改变产品方案。使用用户真实样本优化兼容性、保真度与定位；先开发验证，再代码审查、修复和复验，全部满足后才提交。

## 2026-09-18 当前状态：主题覆盖修复

- 用户已反馈“不再超时”，并提供主题仅覆盖一部分界面的截图；不要继续要求重复确认上一轮超时。最新问题明确落在书架、导航和聊天区主题不一致。
- 已定位到工作台高优先级浅色硬编码、CSS Module 自身颜色、局部主题与根节点不同步这三类原因。不能只看容器背景，标题、空状态、输入框与下拉菜单也要验证。
- 主题统一规则放在 `src/components/workspace-theme.css`，由根布局装配，和同级测试一起维护；没有在 `globals.css` 继续堆叠超过 500 行的补丁。
- 新旧主题变量统一；主页面恢复/切换外观时同步 html 根节点，body 与门户弹层不再留在上一种主题。字体、布局和选文数据不改，书封/彩色标亮保留其原色语义。
- 书架标题、知识库、空状态、聊天标题与消息、会话菜单、插件菜单、字号控件、阅读章节标题及设置主按钮已使用主题角色。阅读标题旧规则存在更高优先级，已在规则来源修复并补回归测试。
- 撤回本轮先前尝试的 EPUB `!important` 文字覆盖；原版文档内部尚缺有效浏览器验收，不把它与截图中的工作台主题修复混为一谈。原生 blob、closed shadow root、sandbox/CSP 均未因主题问题变更。

### 最新验证证据

- 全量开启本地真实 EPUB fixture：112 个测试文件、937 项全部通过，无跳过。日志：`.verify-theme-3286/final-tests.log`。
- 新增工作台主题 CSS 契约测试与真实页面主题同步测试，覆盖六主题恢复、根节点/工作台一致、工作区切换、跨标签事件、存储失败提示、模块局部颜色和章节标题优先级。
- 严格 TypeScript、lint 和 production build 通过。仍保留既有 `data-storage.ts` 动态文件追踪警告，未修改安全存储策略来消除警告。
- 隔离开发端口 3286、隔离生产端口 3287 的首页、设置和书库正常；真实 EPUB 导入 200（58 节/1222 段），后续生产主题验证仅使用自造 TXT，不调用模型。
- CUA 通过用户界面切换六主题，对阅读/书架/知识库/聊天/页脚及标题执行 90 项 computed-style 断言，全部符合预期；最终构建另对六主题正文、章节标题、body/纸面做 24 项复验，全部通过。
- 深色会话菜单、插件菜单、设置页及刷新恢复验证通过。最终设置“保存”按钮为亮青底配炭灰字，而不是低对比度的白字。
- 内置浏览器原版加载仍见超时，截图输出也出现视口裁切/比例不一致；未据此更改产品布局或渲染方案。以上主题证据为 DOM/UI 状态与实际计算样式，不声称完成原版布局的视觉验收。

### 本轮收尾

- 3286/3287 均已结束并查无监听；临时 CUA 标签已关闭，移除了本轮新增的 3286 TypeScript include。此前已有的其他验证配置未擅自删除。
- 现有 3285 服务（PID 4252）保持不动，首页 200；3000/3283 无监听，主项目未由本轮重启。
- 临时验证书库/构建目录留在本机，未绕过此前清理命令限制，也未把个人 EPUB 或验证数据加入 Git。
- 当前任务已对主题修改进行自查，查出的标题优先级与字号按钮颜色问题已修复复验；未声称另一个独立 reviewer 验收了本轮。
- 本轮主题问题修复已落地；此前阅读器大改的完整验收与综合审查仍未收尾，未 commit/push。

## 2026-09-17 历史暂停记录（以以上最新状态为准）

- 已移除 srcdoc 适配和 open shadow root 补丁，恢复原生 blob/closed shadow root；安全、存储和定位修复保留。
- 恢复原生方案后，Codex 内置浏览器仍出现“原版渲染超时，请重试或切回精读”。尚未收到 Chrome/Edge 对照结果，不能判定是工具环境还是产品缺陷。
- 用户于 2026-09-17 说“继续开发”，本轮仅恢复不依赖该环境结论的文本提取修复，不把继续开发当作原版显示已正常的证据。
- 3283 的旧测试页与独立书库继续保留供对照，原先导入的数据仍为 60 节/1294 段；本轮没有改写该书或任何旧标注。
- 原版相关开发、浏览器验收和兼容补丁仍暂停；没有恢复替代运输、开放 shadow root 或放宽安全策略。

## 2026-09-17：独立提取修复已完成并验证

### 产品改动

- 中文目录标签支持排版空白、不换行空格、零宽字符及繁体“目錄”；仅分类标签归一化，不改变正文字符与 UTF-16 偏移。
- 只有明确标记且除标题外全为链接的页面才按导航页过滤。正文中局部 toc、小目录，以及 stock/discovery/cover-image 等类名不再导致整章误删。
- 仅“图书在版编目（CIP）数据”首段与独立 ISBN 字段共同出现、又无章标题的页作为版权元数据过滤，不因正文提及 ISBN/CIP 而删除。
- 无原书标题的内容显示为“未命名章节 N”，不伪称第 N 章。献词、题辞保留；真正的 NCX 数字章名也保留。
- 自动占位标题与正文偶然同文时，不将该正文当作重复标题删除。
- 本轮生产代码仅修改 `src/lib/epub-parser.ts`，配套更新普通/真实样本测试，新增真实依赖合成 EPUB 回归测试；未改原版渲染与存储模块。

### 验证与代码复核

- 全量测试启用用户本地 EPUB fixture：107 个测试文件、876 项全部通过，包含两项真实样本检查。默认 CI 未提供个人文件时会明确跳过真实样本测试。
- lint 与严格 `tsc --noEmit --incremental false` 通过。
- 独立构建目录 `.next-verify-3284` 生产构建通过。保留一条 `data-storage.ts` 动态文件访问导致整项目追踪的构建警告，尚未核对部署影响，也未为消除警告修改存储或编译策略。
- 新隔离端口 3284、独立临时书库：首页与书库 200；真实 EPUB 导入 200；正文与原件读取 200；无效 EPUB 返回非空 422 JSON，书库没有增加残留记录。
- 真实样本：60 节/1294 段变为 58 节/1222 段，只排除了 `part0001.xhtml` 版权页和 `part0002.xhtml` 目录页；全部保留章节逐段与旧版本核对，文字完全一致。献词与题辞保留，前言、第一章、参考文献及 sourceHref 顺序完整。
- 原件 2,191,400 字节，下载与用户输入逐字节一致；SHA-256 为 `20b854d3fcc088a16c01d6f1998325056b8ea78734e83be46c559ed27a0a070a`。没有将个人 EPUB 加入仓库或上传模型/外部服务。
- 测试生产进程停止后重新启动，58 节/1222 段与原件哈希仍一致；3283 旧书 API 数据在验证前后完全一致。
- 本轮由当前任务做代码复核，重点检查整章误删和标题同文数据损失；修正后已包含在上述全量测试与 HTTP 验证中。没有宣称另一个独立审查者已审过本轮改动。
- 新规则仅适用于重新导入，不追溯改写已有版本/标注。以上是文本提取与服务端验收，不等于原版布局、图片、脚注和选区的浏览器验收通过。

## 收尾状态与新增工具限制

- 3284 已停止并通过系统监听列表确认释放。3283 保持运行，首页 HTTP 200；3000 没有监听，主项目未重启。
- 清理本轮临时书库、移除 3284 临时 TypeScript include 的收尾命令被执行工具拒绝（进程未创建）；没有换工具、换 shell 或改写命令绕过。
- 按环境异常约定暂停相关收尾，仅进行了只读状态检查并记录事实。
- 临时书库仍保留在 `C:\Users\NPC-362\AppData\Local\Temp\judu-parser-verify-3284-6285b640d89d4a81b23c2e4b15c2cae6`；3284 的临时 tsconfig include 尚未移除。它们不是用户主书库；待用户确认如何处理工具限制后再收尾。
- `.verify-parser-3284/http-result.json` 记录 HTTP 验证摘要，相关验证目录与日志均被 Git 忽略。

## 尚未完成

- 2026-09-18 用户已反馈不再超时；下一步保留原生方案，补足真实原版显示的有效浏览器验收证据，不重复把内置工具超时当作产品缺陷。
- 真实样本完整的原版样式、图片、脚注、字体与定位/选区回归；安全 CSS 子集和字体限制影响待核对，不放松隔离。
- 选区与原文定位的覆盖和用户反馈优化；跨段/复杂内容尚未宣称支持。
- 核对生产构建动态文件追踪警告，完成被工具阻断的临时数据与配置收尾。
- 真实模型仍未验证，不自动使用用户密钥或收费服务。PDF 原版仍属后续阶段。

## 提交门禁

开发完成并验证成功后进行代码审查；必要问题处理并复验后，才能按 AGENTS.md 的中文 commit message、测试、决策及 Task footer 规范提交并 push。

用户已反馈环境不再超时；原版完整验收、综合审查和此前收尾尚未完成，未 commit/push；HEAD 仍为 `a27b704`。目标继续保持未完成，不以本轮独立修复通过冒充整个阅读器已交付。

## 2026-09-17 设置滚动、灰白主题与引用预览（新请求）

- 设置页 CSS 增加独立 100dvh/overflow-y:auto 滚动区域，未改根页面布局。
- 新增 gray 默认灰白主题；light ID 保留旧配色，中文名改为浅绿，保持已存偏好兼容。bootstrap 默认值同步。
- 新增 epub-link-preview 及同级测试：普通书内锚点悬停/键盘聚焦可预览文本，提供跳转按钮；跨章节只读包内文档；外部地址纯文本展示不访问网络。原版 blob/closed shadow root 与隔离策略不变。
- 定向 4 文件 66 测试通过，严格类型检查及 lint 通过；原版交互仅组件/模块测试通过，未完成真实浏览器验收。尚未运行本轮全量测试或生产构建，未提交/push。
- 隔离端口 3285，构建目录 .next-verify-3285、独立数据目录 judu-ui-verify-3285；/settings HTTP 200。
- 浏览器可见灰白默认选中及浅绿名称，但滚动未生效。只读 DOM 检查 main 实际高度 1808、scrollHeight 1808、overflowY visible，视口高度 1107，与源码 height 100dvh / overflow-y auto 不符。按用户约定暂停环境相关调查，不追加替代样式，不声称设置滚动已验收。
- 3285 与浏览器页面保留供用户用 Chrome/Edge 确认；未释放，主项目未重启。新增临时 tsconfig include 暂保留。本轮没有重试前次被拒绝的清理。

## 2026-09-17 设置滚动根因已修复（用户确认滚轮无效后）

- 浏览器 CSSOM 证据：首条规则 selector 带 U+FEFF，未匹配 main 的 settings 模块 class，导致 height/overflow 均不生效。不是缓存结论，也不再归因为浏览器工具限制。
- 移除 settings.module.css 的 UTF-8 BOM，保留页面独立 100dvh、border-box、overflow-y:auto、flex 不缩小；撤销排查中的内联样式与 !important。全局阅读器布局未改动。
- 3285 实际浏览器刷新后：main 无 inline style，clientHeight 1107、scrollHeight 1928、overflowY auto。真实滚轮操作后 scrollTop 820.90，底部保存按钮位于可见视口；设置页滚动浏览器验收通过。
- 新增首选择器无 BOM/独立滚动约束回归；设置页 3 项测试通过，严格 TypeScript 与 lint 通过。未把该验证扩张为原版引用完整浏览器验收。
- 3285 继续保留给用户检查，未释放；3000 无监听，主项目未重启。未 commit/push。

## 2026-09-17 导入速度优化（实测，不放松校验）

- 导入前端复用提交后的完整响应，经 readBookResponse 同样校验、走原有 loadBook 装配流程；不再额外 GET 整本正文。
- 新客户端用 X-Judu-Import-Response: compact 协商不传无消费者的正文 base64 textHash 副本，老客户端响应契约不变；数据库 text_hash、事务及原件校验完全保留。
- 独立端口 3286、独立 dist/data：真实 2,191,400 字节 EPUB，58 节/1222 段。基线热运行三次导入+重复读取分别 348/317/329ms，新路径热运行 207/208/203ms。这是本机开发模式 API 耗时，不是完整首次可读或跨机器保证。
- 应用层 JSON 正文从 1,377,490 + 626,981 字节变为单次 627,149 字节（不含 HTTP 压缩/请求头）；减少约 69%。首轮额外 GET 的 1022ms 主要是 Next 开发首次编译，不能当生产加速倍数。
- 定向导入/版本/页面交互 45 项通过，明确断言导入后无 /api/books/C 重复请求；类型检查、lint 通过。未做本轮全量或生产构建，未提交 push。
- 3286 已停止，临时 tsconfig include 已移除；3285 保持用户验证服务，3000 主项目未重启。基准结果在忽略的 .verify-speed 中，真实文件仅本机测试。

## 2026-09-18 工作台 UI 优化与子 agent 验收

- 右侧聊天输入区由子 agent Harvey 优化：scoped composer 样式、圆角边框、主题变量、模型/用量标签、发送/停止按钮、底部操作与小屏适配；保留 Enter/Shift+Enter/IME、发送/停止和 aria 行为。
- 新增 `ChatPanelResizer`：桌面拖拽向左增宽、向右缩窄；Arrow/Shift+Arrow/Home/End、双击恢复 390px、localStorage 持久化；≤900px 隐藏，缺少 ResizeObserver/matchMedia 有回退；分隔条 role=separator。
- 中间阅读工具栏新增 `ReaderOptions`：主题、字体、字号、行距、列宽、对齐即时修改并保存，恢复默认和“全部设置”入口；原版布局不受列宽选项影响。
- 独立验收 agent Turing 首轮发现两个阻断：901–1050px 固定列宽 !important 覆盖动态变量、aria-controls 未绑定。已修复：响应式动态 grid 规则补齐 !important/边界，分隔条绑定 `controlsId=chat-panel`，聊天 aside 设置 id。Turing 二次只读复验通过，无新的中高风险问题。
- 真实隔离端口 3287 浏览器验收：工作台、阅读选项、拖拽调宽、键盘调宽均可见且生效；聊天宽度从 400 调至 608，面板实际宽度同步变化；aria-controls 与聊天面板 id 均为 chat-panel。
- 相关 4 文件 81 项测试通过；修正灰白默认主题后全量 **109 个测试文件、915 项通过、2 项跳过**；严格 TypeScript、lint 通过；`git diff --check` 通过（仅 Git 换行提示）。
- 独立生产构建 `.next-verify-3288` 通过，启动验证端口 3288 首页/API 均 200，3288 已释放；3287 已释放，主端口 3000 未启动。此前用户使用的 3285 仍由旧验证服务占用，未在本轮重启或改动。
- 未提交/未 push；仍需用户后续确认视觉偏好后再决定是否进一步微调，当前不以子 agent 代码审查替代最终提交门禁。

## 2026-09-18 圆角与组件组合优化（截图验收未完成）
- 错误提示变为12px圆角状态卡，跟随消息滚动，防止长错误挤占输入区；保留原重试逻辑与alert语义。
- 聊天消息、标题、输入框共用16px对齐线；控件8px、卡片12px、输入区20px圆角；用户消息低饱和中性背景，结构边框统一低对比。
- 阅读底部移除重复分隔线，保留原有字号/翻页功能，按钮使用统一圆角。并未重构原版运输或安全策略。
- 53项聊天/页面交互测试通过，严格类型与lint通过。
- 3291独立验收用真实AnalysisPanel与合成消息/错误，不调用模型。浏览器可访问性树包含消息、错误和输入框，但截图右侧整片空白；按环境确认约定暂停截图验收，不视为已验证、未通过修改渲染绕过。
- 临时verify-round路由已移除；3291释放。主项目使用默认.next和data于3000重启，首页/书库API均200，正式书库未清理。未commit/push。
- 等待用户确认主项目实际显示，再完成灰白/浅绿/雾彩截图验收。未宣称截图验收完成。

## 2026-09-18 圆角截图验收补齐
- 用户确认主项目实际显示正常。普通截图仍出现右侧空白，但文档支持的 fullPage 截图可完整捕获聊天区；只切换截图方式，未修改产品渲染。
- 已实际查看主项目灰白、浅绿、雾彩整页截图：三栏完整、输入框圆角及淡分隔线正常。已恢复原灰白主题，未发送模型请求。
- 隔离3292复用真实AnalysisPanel展示合成用户/助手消息与失败状态。整页截图确认12px错误卡片、重试按钮、对齐留白、20px输入框可见且无重叠；合成状态不表示真实模型故障已修复。
- 临时验收路由移除、3292服务停止，主项目3000保持正常。此前53项相关测试/类型/lint通过。本轮只补截图证据，没有新产品代码变更，未提交push。
