# 第四阶段验收记录

验收日期：2026-09-15（Asia/Shanghai）。本次 Agent、工作台和会话改造已完成本机范围验收；不等于已部署公网，也不代表未实现的向量建库能力已通过验收。

## 验收结论

- 74 个测试文件、527 项测试通过，包括用户提供的两本真实 EPUB；没有跳过该组解析测试。
- ESLint、TypeScript 严格检查和 .next-stage4-release 生产构建通过。
- npm audit --omit=dev：0 个已知依赖告警；这不等于完整公网安全审计。
- 两位复用子 agent 已完成各自范围最终复验，无剩余已确认阻断。没有以不断新开 agent 代替修复复验。
- 隔离端口 3261、3262 完成页面与 API 验证后均已释放。
- 主项目 3000 已重启到经过验证的生产构建，主标签已刷新；正文、书架、历史会话和输入框加载正常。

## 功能与证据

| 要求 | 实现与验证 |
| --- | --- |
| 真正 Agent | LangChain createAgent + typed tools；OpenAI Chat Completions / Claude Messages 原生工具调用，多步执行、纠错与循环上限有协议集成测试 |
| 普通正文与结构结果分开 | raw_delta 只发文字；结构化结果只能来自 save_reading_analysis；工具保存 readingText、概念、拆解和引用；非流式兼容路由复用同一链路，不再强迫正文 JSON |
| 来源可信 | 只允许当前版本、本轮确实提供的片段；逐字验证引用及概念，不允许模型填写保存锚点 |
| 原位重试 | 原两个消息 ID、首次上下文快照、租约与 attemptId 写栅栏；取消后旧结果不能覆盖新尝试 |
| 审计与回放 | 当前与历史工具尝试隔离；成功请求回放恢复工具事件，不重调模型；旧 NULL 归属记录不冒充当前成功 |
| Token | 按步骤结算输入、输出与缓存，累计消耗与最后一步窗口分开；失败也结算已返回 usage，估算明确标识 |
| 长上下文 | 模型通过工具生成中文阅读记忆；按实际序列化输入动态装箱，包含旧摘要与转义开销；完整消息边界保存检查点，不按最近 N 条或比例裁切 |
| 预算变更 | 设置提供输入 200K、400K、1M 三档，输出 1K–16K；服务端兼容旧的 4K–1M 输入预算；显式更新重试预算，不覆盖原始问题、选文与快照 |
| 工作台 | 左侧阅读、书架、知识库；书籍/版本/目录折叠；导入集中到书架，知识库位于中央，右侧继续聊天 |
| 多会话 | 同一版本新建、切换、重命名和刷新恢复；工具卡与检索来源可定位原文 |
| 版本可达 | 实际库 8 books / 8 editions 全部返回，逐一读取 200 且 ID 匹配；不按书名丢弃重复导入记录 |
| 选区隔离 | 搜索 B 后句读不沿用 A 的旧选区；键盘与 selectionchange 选文有回归测试 |
| 图片外传防护 | Markdown 图片不生成 img/preload；HTML 不渲染，配合 CSP、no-referrer 与 nosniff |
| 本机 API 防护 | Host、同源 Origin 与内容类型限制；跨站 POST 403、text/plain JSON 415，同源书架 GET 200 |

## 真实模型与生产验证

首次隔离句读在 3261 返回 1058 次文字增量，首字 3.129 秒，真实执行 search_book 与 save_reading_analysis，保存 7 个概念。浏览器另外验证普通聊天无句读工具卡、句读概念更新、刷新恢复工具卡及出处定位。

最终 .next-stage4-release 在 3262 使用隔离数据库验证：

- HTTP 200、completed；68 次文字增量，首字 4.149 秒，末次增量 20.128 秒。
- read_source 与 save_reading_analysis 成功，正常文本与结构结果并存，保存 1 个概念。
- 输入 6390、输出 618、累计 7008、缓存读取 1536 Token；最后一步窗口占用 2887。
- 成功回放 HTTP 200、replayed=true，恢复 2 个工具事件，始终只有原 2 条消息，审计 attempt 全部匹配。
- 生产浏览器实测新会话、普通聊天、Token 显示；所有测试聊天均在隔离库，未写入真实主库。

真实网关测试使用用户配置的 OpenAI 兼容服务。Claude 使用真实 SDK + 本地 HTTP 协议测试，不冒充已使用真实 Anthropic 账号验证。

## 压缩与独立复验

真实 memory-v2 测试分 4 批覆盖 10 条历史，保留中文、贺麟译本、不以摘要代读的约定及未解问题；下一轮复用检查点，未重复压缩。消息 UUID 不作为原文来源。

该真实测试之后的动态装箱与失败计量修复另有独立回归：旧摘要与新历史原需 4288 Token，修复后分批为 2830、2075，均不超过 4096；length、缺工具和坏参数失败均各结算一次 1724 总量、128 缓存。

- Dirac：工作台、版本入口、搜索选区、旧会话迁移、重试预算与图片外传防护最终复验通过。
- Feynman：会话与工具审计、取消重试、幂等回放、压缩检查点、实际输入装箱和失败 usage 最终复验通过。

## 主库无损切换

- 切换前确认无在途生成并备份真实 SQLite；没有把测试数据库复制回主库。
- 原消息 69 → 69，标注 8 → 8；除修复 thread_id 外，原字段哈希一致。
- 空会话 ID 的 14 条旧消息全部保留，浏览器可打开，输入解锁。
- 备份保存在 data/backups/，具体路径及逐表校验见本机证据，不提交用户数据库。
- 主服务使用 NEXT_DIST_DIR=.next-stage4-release、JUDU_DATA_DIR 指向原 data 目录，只监听 127.0.0.1:3000。

## 证据位置

机器记录位于 Git 忽略目录 .verify-stage4-3261/：

- live-agent-check.json、production-agent-check.json、production-replay-check.json
- memory-live-check.json、migration-check.json、library-version-check.json
- main-cutover-check.json

测试书籍、Key、数据库、备份、构建和服务日志均不进入 Git。最终交付应另外报告实际 Git 提交与 push 结果，不以测试通过代替远端验证。

## 部署与能力边界

- 当前是单用户本机应用，无公网认证、多用户隔离或实际公网部署。非本机使用须另行配置认证、HTTPS、持久卷和 JUDU_APP_ORIGIN；来源限制不等于认证。
- Agent 工具当前使用 SQLite 关键词检索；PostgreSQL/pgvector 查询适配不等于已生成 embedding 或建立全书向量索引。
- 压缩为双协议可移植的模型记忆方案，不宣称 Codex 内部压缩服务的一比一实现。
- 不含 OCR、语音、章节级异步句读或知识包；旧版正文不擅自覆盖重解析。
