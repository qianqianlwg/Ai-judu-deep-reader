# UMD 候选实现与字段来源

日期：2026-09-20。状态：研发候选；没有开放 UMD 生产导入。

## 字段参考

独立实现的字段布局交叉参考 `@file-viewer/renderer-epub@3.1.1` 发布包内 `dist/umd/parser.js`（Flyfish，包内许可证 Apache-2.0）。本项目没有安装整套 renderer、没有复制其 UI、也没有将宽松容错逻辑直接接入生产。

本地核对的发布内容：

- parser SHA-256：`378321fa4ad063ecce3e249b0430652ca8a111f8289af079ee31c87656d7808f`
- LICENSE SHA-256：`55ce5d9fb8138a058b60c431cb9e68e13dd3f90691f73a757e38f06fed81e69b`
- 发布来源：`https://registry.npmjs.org/@file-viewer/renderer-epub/-/renderer-epub-3.1.1.tgz`
- 源码仓库：`https://github.com/flyfish-dev/file-viewer`，对应路径 `packages/renderers/ebook/src/umd/parser.ts`。
- 调研参考提交：`2335490caa812c5a340263c3cf7954ba6dfe9db4`。该提交与发布包尚未做逐字节对应核验，不把两者合并成已证明的来源。

ReadAny/Go 实现仅用于独立核对格式事实，未复制其实现到项目。UMD 格式没有因为两个第三方解析器存在相同分支就完成兼容性证明。

## 与宽松候选不同的决策

- 小端 magic、section/data 长度按真实输入核验；严格边界不能证明一个合法EOF文件在历史上从未被修改，具体profile限制见最终验收。
- 不忽略未知块/标志；保护或授权相关字段语义未确认前明确拒绝，不尝试解密或丢弃。
- 文字型候选用正文索引表校验压缩块的成员完整性，并按正文数据块物理顺序拼接；0x81不是排序表。元数据、数据 ID、标题和章节偏移必须一致。
- 实际解压采用 Node 异步 zlib 的输出预算，不用头部声明代替预算，也不回退 raw-deflate 或吞掉失败。
- UTF-16LE 严格解码，章节边界不可切开代理对；不修正错误偏移、不裁短正文、不用替换字符掩盖损坏。
- 解析进程只能读本次固定私有 bundle，无文件写权限，不继承模型密钥；有超时、取消和 IPC 结果校验。这里的 Node permission 不是完整操作系统/网络沙箱。

## 兼容性边界

当前候选从文字型、多章、正文索引、PNG/JPEG封面开始。漫画型、混合型、其他图片和未知字段仍在第二阶段研究范围内，未验证前拒绝而非静默丢内容。自造 fixture 与原版读取链路测试不冒充独立真实 UMD 文件验收。

生产化还需要原件与派生 EPUB 的双份存储、转换器版本及双哈希关系、导入/知识来源/回跳闭环、合法真实样本与浏览器验证。不得仅将 `.umd` 加入扩展名白名单。

## 2026-09-20 候选代码与首轮门禁

- `umd-container.ts` / `umd-parser.ts`：文字型结构预检、索引顺序、长度/UTF16/受限异步解压；`umd-worker*.ts`：固定私有子进程、超时/取消/IPC校验，禁止写文件。
- `umd-epub.ts`：EPUB2/NCX、固定ID/路径/ZIP时间、正文XML转义、封面完整解码、生成前预算及生成后EPUB安全预检。
- `umd-conversion.ts`：受控装配解析与生成，返回原件哈希、派生物哈希、转换器版本和章节范围；不向生产数据库保存任何内容。
- 发现既有 EPUB 提取器会二次解码部分字面实体、给 `&gt;` 补空格，并过滤与章名同文的段落。未修改旧算法/旧锚点；新增 UMD 精读投影直接使用严格解码的同一快照，DOM Range 回跳验证包含字面实体、HTML样式文本、与标题同文段落。
- 当前自造样本经过真实受限子进程、EPUB生成及旧EPUB读取兼容检查；原件字节没有覆盖。生成 EPUB 另在隔离生产 API 导入成功，两章、下载字节与生成哈希一致。这不是 UMD 原件/派生版双份持久化已完成的证明。
- 全量首轮165文件2342项通过，包含本地真实 EPUB/PDF 回归，无跳过。构建与定向工具检查记录见本地 `.verify-umd-*.log`。
- 3299隔离生产验证：首页/书库200；私有worker与清单HTTP路径404；`.umd`/`.UMD`均415；拒绝请求后书库为空。只将无个人数据的派生EPUB写入隔离书库，未写正式data。服务已停止。
- 真实 UMD 文件矩阵、漫画/混合型、原件与派生版数据库关系和原版浏览器验收仍缺失；UMD导入入口保持415，完整第二阶段尚未完成。

## 2026-09-20 独立验收发现与修复（最终状态）

- 首轮2342项绿测并不足以证明格式正确：独立验收实际发现P1，初版错误地按0x81表的顺序拼接正文。现已修正为成员校验加物理块顺序；逆序ID表的AAAA/BBBB独立探针同时验证正文、canonical、生成EPUB与[0,8)/[8,16)来源区间。错误顺序假设已从fixture及文档删除。
- P2已修复：只为已经核对的81/83/84/0c允许flag=1；其他未知位、F0/F1仍拒绝。字段事实交叉依据为linpinger/txt2ebook-java固定提交8022496b877ae09161fe12a83b98603d74e5f562的UmdChapters/UmdEnd与LZ0211/Wedge固定提交c68b98b196def94f016f37bd7aac269747da09aa的UMD writer，未复制这些实现。
- 显式区分indexed-terminal与simple-eof：前者有81就必须有合法0c，不能在终止缺失时降级；后者无81且无0c、全部flag0、仅已证实的元数据/章节块、必须在正文数据末块结束。两种profile都校验真实展开长度、UTF16、正文完整覆盖与源哈希。
- 独立上游样本已原样随Apache-2.0许可保存在src/lib/fixtures/umd，627字节、固定哈希472dd236088ed386c72234d6aa469f24973a4e8972380ff50429be6fc6f16052，正文380字节，两章[0,208)/[208,380)。parser、真实worker、转换EPUB及6段Range均经独立基准核对；成功样本仍只有这1个无封面的上游自造文本文件。
- 上游样本627个严格前缀全部失败、选定截断输入经worker也失败。若人为删除完整后缀后剩余字节恰好构成另一个合法simple-eof文件，解析器无法推断其历史；不宣称任意UMD的所有截断都必然可检测。
- 子agent最终只读验收：11文件108项全部通过、0跳过；构建器1/1、定向lint、严格tsc/checkJs通过，无本批新增阻断。
- 主流程最终全量166文件2349项通过、0跳过；lint、严格tsc、构建器测试与生产构建通过。原有CSS highlight和data-storage tracing告警未通过改变渲染/安全方案规避。
- 修复后重新在3299隔离生产验证：上游原件UMD仍415；其派生EPUB导入/下载200、两章、SHA-256为51266b7cafdad11ff40a543bd687fbacbabd8c6d92d8fe4329a2dfc3b8574388，往返字节一致。私有runtime路径404，端口已释放。该测试仅证明派生EPUB兼容现有接口，原UMD双份持久化尚未接入。
- 主项目已重启3000、默认.next、正式data；首页/书库API200，重启前后3书/3版本/5条AI标注/2条手动标记/2会话/8消息一致。未为Foliate浏览器问题新增绕过，第二阶段仍未完成。
