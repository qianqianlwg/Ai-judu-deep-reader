# 第二阶段 A：PDF 与原版交互验收记录

日期：2026-09-18
当前状态（2026-09-21）：用户限定的 EPUB/MOBI/PDF 定向收尾目标已完成，详见末尾最终验收。下方按时间保留历史过程，不把早期“待验”视为当前未关闭项；AZW/UMD 等扩展格式及未承诺的全格式兼容仍不计作完成。

## 实现范围

- EPUB 与 PDF 使用共用 OriginalInteractionLayer、AnnotationPopover、ConceptPopoverContent 和 HistoryPopoverContent。概念定义、句末历史标记、完整句读入口和引用卡片与精读保持同一交互设计。
- 浮层不改书内正文节点，不改 CFI，不打开 Foliate closed shadow root，不替换其 blob 运输。
- PDF.js 5.4.296 同版本主线程与本地 worker/CMap/字体/wasm/ICC/图标；无运行时 CDN。
- Canvas 原页、TextLayer 真文字选区、受控 AnnotationLayer；页码、缩放、旋转、文档目录和按原件 hash 保存位置；可见邻页渲染，限制画布像素。
- 新导入按页建立文本索引；旧索引仅整体非空白字符精确一致才衔接，不重写历史 ID，不模糊命中重复文本。
- 跨页选文、五色手动标亮、概念和 AI 下划线分层，整个原生选区最多 1000 Unicode 字符；不会自动拆分成多次句读。
- PDF 原件中的 NUL 会使 SQLite TEXT 读取截断。导入、页索引和文字层统一保留为明确未知符号 �；原件图像不改，不猜公式。
- 图层采用上游要求的 scale-round-x/y 和 userUnit；异步 AnnotationLayer 先在独立节点完成，迟到结果不能污染新缩放；Canvas 取消承诺立即收接。

## 自动化与独立复核

- 本轮最终全量：133 个测试文件、1526 项通过，无跳过；包含本地真实 EPUB 样本及 50 页论文测试。
- 离线资源额外核验：node --test scripts/vendor-pdfjs.test.mjs，3 项通过；worker、许可、CMap、字体、wasm、ICC、图标与固定 npm 包逐字节一致。
- 严格 TypeScript、全项目 ESLint、隔离生产构建通过。构建阶段曾发现可选 page.reference 的类型错误，已修复并重建通过。
- 子 agent 分别负责来源索引/loader、共用交互/引用、PDF 页面/阅读器组件专项。发现的失败断言保留后修复复验；包括同 Document 浮窗提前关闭、外部 UI 键盘劫持、跨 iframe Node realm、版本迟到请求、空 textDiv 归属、UTF16 代理对和无效页引用。
- 全量中包含 React 既有测试的 act 提示；并非失败。构建有 ::highlight 解析告警和动态 fs.open 全项目追踪告警；没有为了消除告警改安全策略，生产 CSS 确认保留高亮规则。部署体积仍待单独优化。

## 真实文件与页面证据

AI 自测端口 3289，隔离生产端口 3290，均使用独立 .verify-phase2-20260918/data，未调用真实模型。

- 用户论文 50 页提取/SQLite 保存读取完全一致，所有页可映射。第 20、21、33 页原有 NUL 被显式表示，后文不再截断。
- 论文原版可见标题、正文和图片/公式；鼠标拖选英文长段落显示“已选 1000 / 1000 字”，继续选取禁用；绿色标亮保存后 API 确认 color=green、1000 字、单锚点。
- 本地合成概念与历史用于验证，不把测试记录写入正式书库；句末标记→摘要→查看完整句读，确实打开对应受控历史正文。概念键盘入口显示独立定义。
- 本地生成双页无个人数据 PDF：真实跨页拖选显示 171 字、2 段；引用先在第 1 页弹出第 2 页概览，点击“跳转到原文”后才跳第 2 页；外部地址只展示，未导航。生产构建重复通过有效引用闭环。
- 250% 放大生产页面显示横向滚动条，页面左侧不再被居中负坐标藏起来。
- 3290 首页/书库/worker 返回 200，论文索引 50 页，原件完整下载 SHA256 与版本一致，错误版本请求 404；当前原件接口完整返回 200，不宣称已支持 HTTP Range 流式下载。

## 真实边界和后续

- 论文目录部分目标为孤立对象（如 num=34），不在实际加载的 50 页对象列表中；独立本地 PDF.js 同样报父页 kids 错误。明确提示原链接可能失效，禁止通过文本猜页。有效引用可按唯一真实页对象定位。
- PDF 引用当前是目标页文字概览，不是精确脚注裁剪。扫描页无 OCR；文本公式不是结构化数学语义，多栏语义阅读顺序仍需更多样本。
- EPUB 概念悬停、固定/关闭、完整历史、引用与键盘已有单元/组件回归；此前 EPUB 内置浏览器超时未由本轮独立实机验证解决，不能据 PDF 正常而宣称 EPUB 实机全过。
- 截至 2026-09-18，本批尚未开展 FB2/FBZ、MOBI/AZW/AZW3、CBZ、UMD 转换验收；MOBI、CBZ 等后续进展以本文件较新日期条目和各格式验收文档为准。
- 保留现有所有未提交工作；本轮未提交/推送，个人文件、临时日志和受控书库不纳入提交。

## 交付环境

- 3289（开发自测）、3290（生产自测）和本批3295（CBZ生产自测）已释放；临时浏览器页已关闭。
- 主项目 D:\project\Ai-judu-deep-reader 已重启：3000、默认 .next、正式 data，未复制或替换成临时书库。首页和书库 API 均返回 200。
- 重启前后统计一致：3 本书、3 个版本、3 条 AI 标注、1 条手动标记、2 个会话、6 条消息。该统计取自本轮实际重启前后，不沿用旧 P0 时的数量。
- 本轮临时 tsconfig include 已清理，既有配置与未提交改动保持。用户只验证主项目 localhost:3000。

## 2026-09-18 后续批次

- B1 FB2/FBZ与B2 CBZ已分别记录；CBZ在3295生产隔离页完成真实图片、下一页、目录、200%缩放、横向滚动和刷新位置恢复。
- 截至该批次，MOBI/AZW/AZW3、UMD 转换尚未开发，EPUB/FB2 Foliate 原生通路仍待环境确认；后续 MOBI 与 EPUB 状态以 2026-09-21 最新验收为准。

## 2026-09-20 共用浮窗后续修复

- 修复 disabled→enabled 及书籍/文档身份切换后旧浮窗复活；concept/history/link 和迟到引用响应都有回归。
- 引用卡片现在显示实际 preview.title，读取中/空标题回退“引用预览”；不可信标题仅为文本节点。
- 子 agent 定向10文件254项通过；沿用真实view.js事件链验证外链兜底，未改变普通内链的原版点击语义。
- 全量154文件2187项、lint、严格类型和生产构建通过。原版实机验收仍未完成，不能把jsdom/引用事件链当成Foliate浏览器视觉证据。
- 本轮启动隔离3296命令被工具环境拒绝，未产生服务、无需释放端口；检查时3000无监听，主项目未重启。等待环境确认，不新增渲染/运输/安全绕过。

## 2026-09-20 原版交互复核与引用截断修复

- EPUB/FB2与PDF在组合根接入各自来源映射，再通过 `OriginalInteractionLayer` 复用精读的 `AnnotationPopover`、`ConceptPopoverContent`、`HistoryPopoverContent`。概念只显示对应定义；已句读末端有历史标识，列出概述并提供“查看完整句读”；引用使用同一浮窗的定位、关闭、焦点及样式，而不是另造阅读器原生弹窗。
- 独立子agent静态装配与18文件442项组件/单元测试通过，发现一个P2：EPUB引用的2400 UTF-16长度截断会切开emoji/增补汉字。主流程先补4个回归用例，实测旧代码2失败/6通过，再按PDF预览已有规则修复截断；相关6文件216项通过。仅修复本地纯文本预览，不改原版渲染、安全策略或正文。
- 当前主项目已加载的EPUB原文和概念高亮实际可见，点击“外部性”入口确实打开独立概念定义卡片，关闭可用。未发送模型请求或创建标注。
- 引用脚注的自动化点击被工具明确拒绝：`Cannot click content inside a closed shadow root`。已停止相关操作、向用户说明并请求在普通浏览器确认，不以解封shadow root、替代blob/srcdoc运输或改安全策略绕过。重启后EPUB位置恢复再次出现20秒渲染超时，尚不能确认是工具环境还是产品通路；不宣称EPUB/FB2全链路实机验收完成。
- 最终全量170文件2500项通过，lint、严格类型和生产构建通过。3300/3301生产隔离HTTP各16项通过（主要为同批UMD存储/书库/下载回归，不冒充概念/引用视觉测试），临时端口均已释放。主项目3000、.next、正式data已重启，首页及两个书库API均200，正式记录摘要全部保持不变。
- 截至该批次，第二阶段仍未完成；UMD 内部双份持久化不等于公开导入/转换版 UI，当时的 MOBI/AZW/AZW3 与实机环境待确认项后来按下方 2026-09-21 最新记录分别关闭或保留。
独立复核补记：修补后的EPUB引用预览由子agent再次只读核对两文件，独立8/8通过，原P2已关闭；未发现新增阻断，不扩大实机环境结论。

## 2026-09-21 Codex 内置浏览器 blob iframe 最小对照

- 在主项目 `http://localhost:3000/` 复现 EPUB 恢复位置超时后，临时给 Foliate paginator 增加只读阶段日志：iframe 已连接，`src` 已成功赋为同源 `blob:http://localhost:3000/...`，但 1 秒和 5 秒后 `contentDocument.URL` 仍为 `about:blank`，没有目标文档 `load` 或 `error` 事件。
- 另建临时最小页面，不经过 EPUB、Foliate、closed shadow、sandbox 或书籍净化链路，分别验证普通 iframe、带 `sandbox="allow-same-origin"` 的普通 iframe、open shadow iframe 和 closed shadow iframe。四种情况均在赋值 blob URL 后 5 秒仍停留 `about:blank`；只有元素插入时的初始空白页 load。
- 因此，当前内置浏览器超时可明确归因到该环境的 blob iframe 导航能力，而不是 sandbox、closed shadow、CFI、具体 EPUB 内容或应用事件监听。临时日志和对照页已删除，产品 blob 运输、安全策略和 Foliate shadow root 未修改。
- 该条记录描述最小对照完成时的状态；后续已改用本机 Chrome 153 Headless/CDP 完成 EPUB/MOBI/PDF 实机验收。产品未新增 srcdoc、服务端章节运输，也未放宽 shadow root、sandbox 或 CSP。

## 2026-09-21 子agent新增缺陷与修复

- 独立子agent在原有绿测试之外新增两个失败探针：普通正文内链可绕过组件操作队列，后发导航 B 先完成后仍可能被迟到的 A 覆盖；MOBI6 在同一个 `li` 内跨 `mbp:pagebreak` 时，会把续文错误推进一个有序列表编号。
- Foliate paginator 现在为每次公开导航和翻页分配单调序号，在章节资源返回、iframe 文档回调、排版后以及提交当前 view 前核对；过期导航以 `AbortError` 终止，不能销毁新 iframe、回退章节或写入旧历史。原生 blob、sandbox、closed shadow root 不变，补丁生成脚本和 PROVENANCE 同步更新。
- MOBI pagebreak 上下文只对分页点前已经闭合的直属 `li` 推进序号；分页点仍位于当前 `li` 内时，续章恢复当前 `start/value`。新增普通项间、项内、`value`、`reversed` 和嵌套列表回归，并通过真实发布链确认续文与下一完整项均不偏移。
- 修复后 EPUB/MOBI 定向 6 文件 107 项通过；带真实 EPUB 与 50 页 PDF 样本的全量回归为 206 文件、3508 项通过，无跳过。同一子agent原失败探针 2/2、生命周期/CFI/导航/MOBI 定向 13 文件 213 项和真实发布链列表矩阵 7/7 均通过，两个具体缺陷已关闭。
- 当前 HEAD 的隔离生产构建通过；3314 端口完成 EPUB/MOBI/PDF 导入、原件字节、MOBI 布局、固定 Foliate 分发和私有路径边界共 15 项 HTTP 检查，随后已释放。构建仍只有既有 `::highlight` 与动态文件追踪警告。

## 2026-09-21 本机 Chrome 三格式最终验收

- 环境：本机 Chrome 153 Headless/CDP，区别于 Codex 内置浏览器。EPUB 使用主项目 `http://localhost:3000/` 与正式书库只读验收；MOBI/PDF 使用隔离生产端口 3315 和独立数据目录，不调用真实模型。
- EPUB：Foliate 原版稳定超过 20 秒，无超时；完成下一页、目录从章节 8 跳到 11、刷新后仍为章节 11 且显示进度一致。精确 CFI 由定向回归覆盖，本次页面日志不把章节恢复扩大为精确页内位置证明。真实页面打开“外部性”概念定义、`(9)` 引用浮窗与目标地址、句读历史摘要及“查看完整句读”，并恢复原线程、完整回答和定位入口；引用正文正确性与跳转未计入本次实机通过项。
- MOBI：真实固定样本稳定加载，章节 1/2 与 2/2 的翻页/目录、PDF→MOBI 切书恢复和 blob iframe 均通过。原版选择 `Libmobi features` 映射为 16/1000 字；1295 个 Unicode 字符被明确限制为 1000/1000 字，句读入口仍可用。
- PDF：用户 50 页论文显示两个 `327 × 462` Canvas、TextLayer 中英文文字与页码；点击下一页后当前页从 1 到 2，第 3 页作为邻页预加载节点出现。缩放、旋转控件存在但本次未执行操作，目录未列入本次实机通过项。仅有既有 CambriaMath 字体回退警告，无运行时错误。
- 自动化：206 个测试文件、3508 项通过，无跳过；TypeScript、ESLint、生产构建、真实 EPUB/PDF、子 agent 原失败探针 2/2、定向 13 文件 213 项和 MOBI 发布链列表矩阵 7/7 均通过。
- 数据与环境：3315、9333 已释放；主项目 3000 首页和书库 API 均 200，书库 3 本。正式 SQLite 的 13 张业务表数量和逐表 SHA-256 摘要与验收前一致。主项目保持运行，用户只需验证 `http://localhost:3000/`。
- 结论：用户优先的 EPUB、MOBI、PDF 已达到本轮交付门槛。Codex 内置浏览器 blob iframe 停留 `about:blank` 是工具环境限制，不是普通 Chrome 产品通路失败；没有为工具增加替代运输或降低安全边界。完整复杂 MOBI 版式、AZW/AZW3、UMD、FB2 等仍按各自边界单列。

## 2026-09-21 恢复目标：脚注正文修复与 MOBI 版式回归

- 实际 EPUB 的引用目标为 `<p><a id="…">(9)</a>解释正文</p>`，旧实现只提取编号。新增有界上下文解析：单条 `footnote/endnote`、`doc-footnote/doc-endnote` 语义容器或最近正文单元；空锚点可回退至没有块级后代的局部 aside/div。不把复数脚注集合或整章当作单条注释，不改 DOM、fragment 或章节 index。
- EPUB 类型属性优先按命名空间 URI 读取，兼容字面 `epub:type`。独立 agent 发现的别名前缀和旧式空锚点两个问题已修复；原 17 项探针全部通过，引用/共用交互 3 文件 150 项通过。
- 使用正式 EPUB 原件的只读本地 XHTML 核验：预览与目标段落文字完全一致，为 184 字，原 fragment 和目标章节保留；没有向模型或第三方上传文件。该证据不是实际页面点击或跳转证据。
- MOBI6 发布链新增/增强普通段落、嵌套引用、重复文字、标题和跨页静态 PNG 测试。图片由 Sharp 生成并真解码，资源字节、alt、祖先和图文结构均核对；发布矩阵 9 项、MOBI 定向 6 文件 61 项通过。没有修改 MOBI 产品逻辑。
- PDF/EPUB 生命周期、位置和链接定向 5 文件 360 项通过；严格 TypeScript、改动文件 ESLint、隔离生产构建通过。未重复运行上轮 3508 项全量，不把旧数量写成本轮新结果。
- 隔离端口 3316 使用独立构建/数据，完成 EPUB/MOBI/PDF 导入、原件逐字节一致和 MOBI 布局 API；生产构建另完成首页、书库与三份原件哈希共 5 项 HTTP 复验。构建保留既有 CSS highlight/动态文件追踪警告。
- 独立 Chrome 启动命令被工具策略拒绝，浏览器并未启动。已暂停相关实机验收并请用户确认环境，不尝试替代运输或降低产品安全策略。EPUB 引用跳转/精确 CFI、PDF 缩放旋转目录和 MOBI 新矩阵视觉验收继续保留。
- 交付状态：3316 已释放，9334 未启动且无监听；主项目 3000、默认 .next、正式 data 已重启，首页和书库 API 均 200，3 本书及 13 张业务表数量/摘要与本批前一致。本批未修改正式数据、未调用真实模型。

## 2026-09-21 浏览器工具复验：PDF位置恢复与末页导航

- 用户指定 `@浏览器` 后，使用其实际连接的 Codex 内置浏览器在隔离生产端口 3317 验收，没有再次尝试终端启动 Chrome。用户确认可以看到 PDF 正文；工具截图空白与该反馈不一致，未据此更换渲染方案。
- 实际发现两项 PDF 产品问题，并先用独立组件回归复现：有效保存页被未变化的初始精读锚点覆盖；短末页在缩放/旋转后，浏览器钳制 scrollTop 的事件被误判为前一页。旧代码分别出现 4 项恢复失败及 8 项导航/旋转失败，均保留回归。
- 修复：保存位置附带 canonical 锚点，同值初始锚点不覆盖恢复页，主动改变锚点仍导航；记录浏览器实际接受的导航滚动落点，离开落点后恢复正常滚动判页，滚到底部识别末页。保留 edition/hash 校验、切书清理及迟到任务守卫，不改 PDF.js 渲染、目录解析或安全策略。
- 旧位置记录没有 anchor 字段时优先恢复保存页；这种旧记录本身无法区分首次传入锚点是否代表主动定位，新保存记录已包含该信息。此兼容取舍不伪装成旧数据可以恢复不存在的意图。
- 新合成双页 PDF 使用真实 Outlines 和页面对象目标，导入前由实际 PDF.js 验证两个目录分别指向第 1/2 页。修复后的生产页面通过原生可访问性接口实际操作：目录到第 2 页 → 200% → 150% → 旋转 → 刷新，最终页码仍为 2、缩放为 150%；渲染文字层 `data-main-rotation=270`，画布 CSS 为 `960 × 720`，与横向状态一致。不是只检查控件存在。
- PDF Reader 60 项、PDF 相关 9 文件 385 项通过；主流程复跑 Reader/Page/Links/OriginalReader 共 4 文件 191 项通过。复用另一既有 agent 只读核验 saved.anchor，18 项独立探针与 35 项范围筛选回归通过，25 项未纳入该子审查范围，不把它们计作该次通过。
- 严格 TypeScript、改动文件 ESLint、隔离生产构建与首页/书库 HTTP 检查通过；本轮不重复宣称上轮 3508 项全量已重新执行。
- EPUB 在该内置浏览器仍出现“加载原版首章”20 秒超时。其引用正文/跳转、精确 CFI，以及新增 MOBI 版式的浏览器验收仍保留；不以 PDF 正常来关闭 Foliate 验收，不修改 blob、closed shadow root、sandbox 或 CSP。
- 交付：3317 已释放，9334 无监听，自测页已关闭；主项目 3000、默认 .next、正式 data 已重启，首页和书库 API 均 200，3 本书且 13 张业务表摘要与本批开始一致。未调用真实模型、未写正式书库。

## 2026-09-21 用户确认 EPUB 可读与主页面引用核对

- 用户明确确认主项目 EPUB 原版可见并能翻页；主页面截图中正文、概念高亮和句读历史标记也实际可见。这只关闭“用户主页面是否能阅读”的环境疑问，不等同于引用跳转、精确恢复和全部 MOBI 版式已验收。
- 新开的 3318 隔离页只使用浏览器原生可访问性接口，不调用 Playwright，仍出现“加载原版首章”超时；3318 已释放。已有主标签正常与新开隔离页异常并存，早期“内置浏览器不支持 blob iframe”的泛化结论应收窄到当时的自动化对照环境。
- 在已正常加载的主页面观察到脚注 19 的“引用预览”：包含完整解释和目标 `OEBPS/Text/part0014.xhtml#jz_2_29`，文字与本地 EPUB 对应段落完全一致，不再仅显示编号。未调用模型或新增标注。
- 浏览器坐标点击正文脚注返回 `Cannot click content inside a closed shadow root`；没有开放 shadow root 或使用替代运输绕过。对应引用浮窗仍可观察到，点击应用自身的“跳转到原文”按钮后出现“原版渲染超时（跳转引用）”，因此本次跨章节跳转没有通过。
- 该结果可能涉及新文档加载或工具环境，不能凭一般翻页正常断定跨章节跳转已好，也不能未经独立复现就直接改 Foliate/安全策略。当时需要确认同一脚注的用户手动跳转；该疑问已由下方最新用户反馈关闭。
- 为避免留下错误页，已通过现有“切回精读”入口恢复阅读，保留同一章节，恢复核验前聊天原文折叠状态；不刷新或关闭用户主标签。正式数据核对另记录于本批交付结果。
- 本批交付：13 张正式业务表数量及摘要核验前后一致；本轮仅修订证据文档，未改变产品代码、未重复重启主服务。主项目 3000 保持运行；3318 已释放，未向用户交付隔离地址。

## 2026-09-21 用户确认引用跳转流畅

- 针对前条记录同一脚注的“跳转到原文”，用户明确反馈“不会超时，立马跳转，很流畅”。据此将该主项目手动验收项记为通过，不再重复询问正文是否可读、翻页或引用跳转是否超时。
- 这是用户实际操作反馈，不伪称自动化点击通过；保留自动化的历史失败作为工具与用户操作表现差异，尚无独立证据证明当前产品跳转存在可复现缺陷。因此不更改 Foliate 渲染、文档运输、closed shadow root、sandbox、CSP 或超时时长。
- 此前本地真实资源/CFI 探针 2 项通过，组件导航及生命周期复核 87 项通过；这些只提供独立逻辑证据，不替代浏览器精确页内位置恢复或复杂版式视觉验收。
- 仍待完成：EPUB 刷新前后同一页内正文位置核对；MOBI 新增嵌套引用、标题、图片及列表跨页的实际分页显示。PDF 目录、缩放、旋转与刷新恢复沿用已完成的实机证据，不重复全量回归。
- 本轮交付检查：首页、`/api/library`、`/api/books` 均为 200，正式书库仍为 3 本；3316/3317/3318 均无监听，9334 未启动。本轮没有新起验证服务，也没有重启或刷新用户主页面。13 张正式业务表数量/摘要核对前后一致。仅提交本轮 4 份验收文档，不混入同时出现的其他未提交业务代码，也不将新增未验工作算作通过。

## 2026-09-21 用户确认精确位置恢复并交付主书架 MOBI 样本

- 用户明确反馈 EPUB “刷新后是同一句”，据此关闭当前主项目页内位置恢复的手动验收项；不伪称工具读取了精确 CFI，也不扩展为所有书籍、字体及视口组合均已验收。
- 此前 MOBI 六章样本只导入隔离书库，用户在主项目中无法找到，交付入口不完整。现按用户要求，经主项目 3000 的标准导入接口将合成 MOBI6 样本添加到正式书架，名称为「MOBI复杂版式定向验收」，文件名「MOBI复杂版式定向验收.mobi」。样本不含第三方书籍或个人内容，不重复导入同名同哈希文件。
- 交付检查：书库由 3 本增为 4 本；新增 1 本书、1 个版本、6 章和 8 个段落，导入前已有的 13 张业务表全部旧记录逐行摘要保持不变。原件下载与样本逐字节一致，MOBI 布局 API 返回 6 章、1 份共享静态 PNG。浏览器点击主项目“书架”及“刷新书架”后，书名、`.MOBI` 版本和打开阅读入口均实际可见。
- 用户验收步骤：打开该书并选择“原版”；第 1/2 章核对引用缩进及蓝色左边线是否跨章保留；第 3/4 章核对列表编号 4、5 后续为 6，另一个条目仍为显式 12；第 5/6 章均应显示上蓝下金的双色图片及各自图注，而不是仅剩文字。原书样本已在主书架，不要求用户进入隔离端口或子项目。
- 这些导入、字节和入口检查不等于 MOBI 复杂版式视觉通过，仍等待该项实际验收。此前隔离 3319 的首章加载超时保留为工具环境差异证据，不修改文档运输、shadow root 或安全策略来绕过。
- 本轮仅更新验收文档并导入用户要求的样本，无产品代码变更；不混入其他未提交业务改动。主项目保持默认 `.next`、正式 `data` 和 3000，本轮未重启；临时验证端口已释放，无需为新增一本书重启服务。

## 2026-09-21 三格式定向收尾最终验收

- 用户对主书架「MOBI复杂版式定向验收」的六章逐项检查明确回复“可以，都是正常的继续完成”。据此记录跨页嵌套引用的缩进/边线、列表 4/5→6 及显式 12、两章蓝金双色图片和图注全部通过。该证据为用户实际操作反馈，不改写为自动化视觉成功。
- EPUB：脚注正文已与原件核对，用户确认“跳转到原文”立即完成且无超时；用户确认刷新后仍为同一句，当前页内位置恢复通过。单条脚注提取修复位于已提交的 063b178；本地 DOM/CFI 探针只提供补充证据，不替代人工验收。
- PDF：目录到第 2 页、缩放 200%→150%、旋转及刷新后仍第 2 页/150% 已由此前真实页面操作验证。初始锚点覆盖保存页和旋转短末页回退修复位于 b37598c；此前四文件 191 项回归及隔离生产构建证据保留，不伪称本轮重新执行全量测试。
- 完整目标核对：三格式四组定向验收均已完成；可复现的引用预览/PDF 位置问题已修复并保留回归；既有 agent 独立审查证据及本轮复用验收单独列明，未新增 agent；没有扩展 AZW/UMD，也未为工具超时修改 blob、closed shadow root、sandbox、CSP 或超时时长。
- 主项目交付检查：3000 首页及两个书库 API 均 200，书架 4 本，MOBI 样本仅 1 本，其下载字节与正式 data 原件一致。前述产品修复批次已从隔离自测交付并重启主项目；本轮只有文档收尾，不再打断用户已验收的主服务。3316/3317/3318/3319 均无监听，9334 未启动；不提供隔离地址给用户。
- 本轮复用既有 agent 做最后一次有界回归：`epub-link-preview.test.ts` 21 项、`mobi-pagebreak-layout.test.ts` 9 项、`pdf-reader.test.tsx` 60 项，共 3 文件 90 项全部通过（退出码 0）。实际验证基于 HEAD `fdfab2d` 的当前工作树，包含其他任务未提交改动，不冒充纯提交快照；修复逻辑仍在，测试期间对应实现/测试文件哈希未变化。未新增 agent、未全量构建、未操作浏览器或重启服务。
- 最终文档检查通过；收尾前后 13 张正式业务表的数量及摘要完全一致，书库保留用户已验收的第 4 本合成 MOBI。其余未提交业务代码不在本次文档提交范围内，也不作全量验收通过声明。
- 完成边界：这是原目标的 EPUB/MOBI/PDF 定向收尾完成，不等于所有书籍、视口、KF8 复杂版式、动画/字体/音视频、OCR 或扩展格式全部支持。工作区其他未提交业务改动仍属于其他批次，不包含在本次提交与完成声明中。
