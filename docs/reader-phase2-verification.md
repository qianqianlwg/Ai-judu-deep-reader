# 第二阶段 A：PDF 与原版交互验收记录

日期：2026-09-18
状态：本批代码、真实 PDF 和隔离生产构建通过；第二阶段整体尚未完成。

## 实现范围

- EPUB 与 PDF 使用共用 OriginalInteractionLayer、AnnotationPopover、ConceptPopoverContent 和 HistoryPopoverContent。概念定义、句末历史标记、完整句读入口和引用卡片与精读保持同一交互设计。
- 浮层不改书内正文节点，不改 CFI，不打开 Foliate closed shadow root，不替换其 blob 运输。
- PDF.js 5.4.296 同版本主线程与本地 worker/CMap/字体/wasm/ICC/图标；无运行时 CDN。
- Canvas 原页、TextLayer 真文字选区、受控 AnnotationLayer；页码、缩放、旋转、文档目录和按原件 hash 保存位置；可见邻页渲染，限制画布像素。
- 新导入按页建立文本索引；旧索引仅整体非空白字符精确一致才衔接，不重写历史 ID，不模糊命中重复文本。
- 跨页选文、五色手动标亮、概念和 AI 下划线分层，整个原生选区最多 1000 Unicode 字符；不会自动拆分成多次句读。
- PDF 原件中的 NUL 会使 SQLite TEXT 读取截断。导入、页索引和文字层统一保留为明确未知符号 �；原件图像不改，不猜公式。
- 图层采用上游要求的 scale-round-x/y 和 userUnit；异步 AnnotationLayer 先在独立节点完成，迟到结果不能污染新缩放；Canvas 取消承诺立即收接。

## 自动化与独立复核

- 本轮最终全量：133 个测试文件、1526 项通过，无跳过；包含本地真实 EPUB 样本及 50 页论文测试。
- 离线资源额外核验：node --test scripts/vendor-pdfjs.test.mjs，3 项通过；worker、许可、CMap、字体、wasm、ICC、图标与固定 npm 包逐字节一致。
- 严格 TypeScript、全项目 ESLint、隔离生产构建通过。构建阶段曾发现可选 page.reference 的类型错误，已修复并重建通过。
- 子 agent 分别负责来源索引/loader、共用交互/引用、PDF 页面/阅读器组件专项。发现的失败断言保留后修复复验；包括同 Document 浮窗提前关闭、外部 UI 键盘劫持、跨 iframe Node realm、版本迟到请求、空 textDiv 归属、UTF16 代理对和无效页引用。
- 全量中包含 React 既有测试的 act 提示；并非失败。构建有 ::highlight 解析告警和动态 fs.open 全项目追踪告警；没有为了消除告警改安全策略，生产 CSS 确认保留高亮规则。部署体积仍待单独优化。

## 真实文件与页面证据

AI 自测端口 3289，隔离生产端口 3290，均使用独立 .verify-phase2-20260918/data，未调用真实模型。

- 用户论文 50 页提取/SQLite 保存读取完全一致，所有页可映射。第 20、21、33 页原有 NUL 被显式表示，后文不再截断。
- 论文原版可见标题、正文和图片/公式；鼠标拖选英文长段落显示“已选 1000 / 1000 字”，继续选取禁用；绿色标亮保存后 API 确认 color=green、1000 字、单锚点。
- 本地合成概念与历史用于验证，不把测试记录写入正式书库；句末标记→摘要→查看完整句读，确实打开对应受控历史正文。概念键盘入口显示独立定义。
- 本地生成双页无个人数据 PDF：真实跨页拖选显示 171 字、2 段；引用先在第 1 页弹出第 2 页概览，点击“跳转到原文”后才跳第 2 页；外部地址只展示，未导航。生产构建重复通过有效引用闭环。
- 250% 放大生产页面显示横向滚动条，页面左侧不再被居中负坐标藏起来。
- 3290 首页/书库/worker 返回 200，论文索引 50 页，原件完整下载 SHA256 与版本一致，错误版本请求 404；当前原件接口完整返回 200，不宣称已支持 HTTP Range 流式下载。

## 真实边界和后续

- 论文目录部分目标为孤立对象（如 num=34），不在实际加载的 50 页对象列表中；独立本地 PDF.js 同样报父页 kids 错误。明确提示原链接可能失效，禁止通过文本猜页。有效引用可按唯一真实页对象定位。
- PDF 引用当前是目标页文字概览，不是精确脚注裁剪。扫描页无 OCR；文本公式不是结构化数学语义，多栏语义阅读顺序仍需更多样本。
- EPUB 概念悬停、固定/关闭、完整历史、引用与键盘已有单元/组件回归；此前 EPUB 内置浏览器超时未由本轮独立实机验证解决，不能据 PDF 正常而宣称 EPUB 实机全过。
- FB2/FBZ、MOBI/AZW/AZW3、CBZ、UMD 转换及其许可/真实样本验收尚未开展，不对外宣称这些格式已支持。
- 保留现有所有未提交工作；本轮未提交/推送，个人文件、临时日志和受控书库不纳入提交。

## 交付环境

- 3289（开发自测）、3290（生产自测）和本批3295（CBZ生产自测）已释放；临时浏览器页已关闭。
- 主项目 D:\project\Ai-judu-deep-reader 已重启：3000、默认 .next、正式 data，未复制或替换成临时书库。首页和书库 API 均返回 200。
- 重启前后统计一致：3 本书、3 个版本、3 条 AI 标注、1 条手动标记、2 个会话、6 条消息。该统计取自本轮实际重启前后，不沿用旧 P0 时的数量。
- 本轮临时 tsconfig include 已清理，既有配置与未提交改动保持。用户只验证主项目 localhost:3000。

## 2026-09-18 后续批次

- B1 FB2/FBZ与B2 CBZ已分别记录；CBZ在3295生产隔离页完成真实图片、下一页、目录、200%缩放、横向滚动和刷新位置恢复。
- 第二阶段仍未完成：MOBI/AZW/AZW3、UMD转换尚未开发；EPUB/FB2 Foliate原生通路的此前超时环境仍待确认。

## 2026-09-20 共用浮窗后续修复

- 修复 disabled→enabled 及书籍/文档身份切换后旧浮窗复活；concept/history/link 和迟到引用响应都有回归。
- 引用卡片现在显示实际 preview.title，读取中/空标题回退“引用预览”；不可信标题仅为文本节点。
- 子 agent 定向10文件254项通过；沿用真实view.js事件链验证外链兜底，未改变普通内链的原版点击语义。
- 全量154文件2187项、lint、严格类型和生产构建通过。原版实机验收仍未完成，不能把jsdom/引用事件链当成Foliate浏览器视觉证据。
- 本轮启动隔离3296命令被工具环境拒绝，未产生服务、无需释放端口；检查时3000无监听，主项目未重启。等待环境确认，不新增渲染/运输/安全绕过。

## 2026-09-20 原版交互复核与引用截断修复

- EPUB/FB2与PDF在组合根接入各自来源映射，再通过 `OriginalInteractionLayer` 复用精读的 `AnnotationPopover`、`ConceptPopoverContent`、`HistoryPopoverContent`。概念只显示对应定义；已句读末端有历史标识，列出概述并提供“查看完整句读”；引用使用同一浮窗的定位、关闭、焦点及样式，而不是另造阅读器原生弹窗。
- 独立子agent静态装配与18文件442项组件/单元测试通过，发现一个P2：EPUB引用的2400 UTF-16长度截断会切开emoji/增补汉字。主流程先补4个回归用例，实测旧代码2失败/6通过，再按PDF预览已有规则修复截断；相关6文件216项通过。仅修复本地纯文本预览，不改原版渲染、安全策略或正文。
- 当前主项目已加载的EPUB原文和概念高亮实际可见，点击“外部性”入口确实打开独立概念定义卡片，关闭可用。未发送模型请求或创建标注。
- 引用脚注的自动化点击被工具明确拒绝：`Cannot click content inside a closed shadow root`。已停止相关操作、向用户说明并请求在普通浏览器确认，不以解封shadow root、替代blob/srcdoc运输或改安全策略绕过。重启后EPUB位置恢复再次出现20秒渲染超时，尚不能确认是工具环境还是产品通路；不宣称EPUB/FB2全链路实机验收完成。
- 最终全量170文件2500项通过，lint、严格类型和生产构建通过。3300/3301生产隔离HTTP各16项通过（主要为同批UMD存储/书库/下载回归，不冒充概念/引用视觉测试），临时端口均已释放。主项目3000、.next、正式data已重启，首页及两个书库API均200，正式记录摘要全部保持不变。
- 第二阶段仍未完成；UMD内部双份持久化不等于公开导入/转换版UI，MOBI/AZW/AZW3以及实机环境待确认项仍保留。
独立复核补记：修补后的EPUB引用预览由子agent再次只读核对两文件，独立8/8通过，原P2已关闭；未发现新增阻断，不扩大实机环境结论。

## 2026-09-21 Codex 内置浏览器 blob iframe 最小对照

- 在主项目 `http://localhost:3000/` 复现 EPUB 恢复位置超时后，临时给 Foliate paginator 增加只读阶段日志：iframe 已连接，`src` 已成功赋为同源 `blob:http://localhost:3000/...`，但 1 秒和 5 秒后 `contentDocument.URL` 仍为 `about:blank`，没有目标文档 `load` 或 `error` 事件。
- 另建临时最小页面，不经过 EPUB、Foliate、closed shadow、sandbox 或书籍净化链路，分别验证普通 iframe、带 `sandbox="allow-same-origin"` 的普通 iframe、open shadow iframe 和 closed shadow iframe。四种情况均在赋值 blob URL 后 5 秒仍停留 `about:blank`；只有元素插入时的初始空白页 load。
- 因此，当前内置浏览器超时可明确归因到该环境的 blob iframe 导航能力，而不是 sandbox、closed shadow、CFI、具体 EPUB 内容或应用事件监听。临时日志和对照页已删除，产品 blob 运输、安全策略和 Foliate shadow root 未修改。
- 当前 Codex 浏览器清单没有可用 Chrome/Edge 自动化连接；本机虽安装浏览器，但按环境约定不绕过受控浏览器工具启动。下一步仍须由用户在普通 Chrome/Edge 主项目中确认 EPUB/MOBI；确认前不新增 srcdoc、服务端章节运输或放宽 sandbox/CSP。

## 2026-09-21 子agent新增缺陷与修复

- 独立子agent在原有绿测试之外新增两个失败探针：普通正文内链可绕过组件操作队列，后发导航 B 先完成后仍可能被迟到的 A 覆盖；MOBI6 在同一个 `li` 内跨 `mbp:pagebreak` 时，会把续文错误推进一个有序列表编号。
- Foliate paginator 现在为每次公开导航和翻页分配单调序号，在章节资源返回、iframe 文档回调、排版后以及提交当前 view 前核对；过期导航以 `AbortError` 终止，不能销毁新 iframe、回退章节或写入旧历史。原生 blob、sandbox、closed shadow root 不变，补丁生成脚本和 PROVENANCE 同步更新。
- MOBI pagebreak 上下文只对分页点前已经闭合的直属 `li` 推进序号；分页点仍位于当前 `li` 内时，续章恢复当前 `start/value`。新增普通项间、项内、`value`、`reversed` 和嵌套列表回归，并通过真实发布链确认续文与下一完整项均不偏移。
- 修复后 EPUB/MOBI 定向 6 文件 107 项通过；带真实 EPUB 与 50 页 PDF 样本的全量回归为 206 文件、3508 项通过，无跳过。同一子agent原失败探针 2/2、生命周期/CFI/导航/MOBI 定向 13 文件 213 项和真实发布链列表矩阵 7/7 均通过，两个具体缺陷已关闭。
- 当前 HEAD 的隔离生产构建通过；3314 端口完成 EPUB/MOBI/PDF 导入、原件字节、MOBI 布局、固定 Foliate 分发和私有路径边界共 15 项 HTTP 检查，随后已释放。构建仍只有既有 `::highlight` 与动态文件追踪警告。
