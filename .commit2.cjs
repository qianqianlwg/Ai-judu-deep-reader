const git=require('isomorphic-git'),fs=require('fs'),dir=process.cwd();(async()=>{for(const [filepath,head,worktree,stage] of await git.statusMatrix({fs,dir})){if(worktree!==0&&filepath!=='data/judu.sqlite')await git.add({fs,dir,filepath});}console.log(await git.commit({fs,dir,author:{name:'Codex',email:'codex@local'},message:`chore(repo): 落实 AI 开发约定并稳定数据库初始化\n\n开发详情：延迟初始化内置 SQLite，增加严格类型、同级单元测试、AGENTS.md 开发约定，并移除 better-sqlite3 原生依赖。\n\n测试：单元测试 + 构建检查\n- 单元测试：npm test\n- 静态检查：npm run lint\n- 构建检查：npm run build\n\n被否决：不引入原生 C++ 编译依赖。\n决策：使用 Node.js 内置 SQLite，降低 Windows 本地调试门槛。\n\nTask: TASK-001\nAssisted-by: Codex`}));})();
