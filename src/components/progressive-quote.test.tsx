// @vitest-environment jsdom
import { act } from "react";import {createRoot} from "react-dom/client";import {expect,it,vi} from "vitest";
import { ProgressiveQuote,quoteBlocks } from './progressive-quote';
it('编号和自然段分点显示，拼接逐字不变',()=>{for(const text of ['标题\n\n（1）第一点。（2）第二点。','Emoji 😀\n下一段','没有列表的原文'])expect(quoteBlocks(text).join('')).toBe(text);expect(quoteBlocks('说明。（1）第一点。（2）第二点。')).toHaveLength(3);});
it('长文初始只挂载节选，按需展开再收起；换文重置且定位用文本不受影响',async()=>{vi.stubGlobal('IS_REACT_ACT_ENVIRONMENT',true);const host=document.createElement('div'),root=createRoot(host),text='😀'.repeat(300)+'末尾原文';try{await act(async()=>root.render(<ProgressiveQuote text={text}/>));expect(host.querySelector('blockquote')!.textContent).toBe('😀'.repeat(220));expect(host.textContent).not.toContain('末尾原文');await act(async()=>host.querySelector('button')!.click());expect(host.querySelector('blockquote')!.textContent).toBe(text);expect(host.querySelector('button')!.getAttribute('aria-expanded')).toBe('true');await act(async()=>host.querySelector('button')!.click());expect(host.textContent).not.toContain('末尾原文');await act(async()=>root.render(<ProgressiveQuote text='短原文'/>));expect(host.querySelector('button')).toBeNull();expect(host.textContent).toBe('短原文');}finally{await act(async()=>root.unmount());vi.unstubAllGlobals();}});
